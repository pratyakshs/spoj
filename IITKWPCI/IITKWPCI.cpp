#include<bits/stdc++.h>
using namespace std;

int main(){
    int t;
    scanf("%i", &t);
    while(t--){
        int n, m;
        scanf("%i %i", &n, &m);
        int a[n];
        for(int i=0; i<n; i++){
            scanf("%i", &a[i]);
        }
        vector<vector<int> > adjacency[n];
        for(int i=0; i<m; i++){
        }

    }
}
